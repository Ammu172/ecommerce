version: "3"
services:
  backend:
     container_name: backend_cont
     build:
       context: .
     ports:
      - "5000:5000"
  frontend:
     container_name: frontend_cont
     build:
       context: .
     ports:
       - "3000:80"
